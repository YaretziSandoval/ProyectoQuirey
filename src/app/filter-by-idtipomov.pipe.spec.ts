import { FilterByIdtipomovPipe } from './filter-by-idtipomov.pipe';

describe('FilterByIdtipomovPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIdtipomovPipe();
    expect(pipe).toBeTruthy();
  });
});
